import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'auditorias_sonoco',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
